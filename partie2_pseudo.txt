Ceci est le pseudocode pour la partie 2 du projet

test_hypertree(hg)
  afficher_dual(hg)
  # tester si le hg est alpha-acyclqiue
  primal = to_primal(hg)
  bool_cordal = is_cordal(hg)
  # c contient les cliques maximales du graphe primal
  c = get_max_cliques(primal)
  bool_cycles = test_cycles(c)
  return bool_cycles and bool_cordal


  
afficher_dual(hg)
  afficher le graphe biparti du hg

test_cycles(c)
  for cycles in c:
    test_if_hyperarete(cycles) # très vague
  return bool

to_primal(hg)
  # prend hg en para et renvoie le graphe primal de celui

  TO DO

is_cordal(gp)
  # prend un graphe primal en para en renvoie un bool qui dit si
  # si il est cordal ou pas

  TO DO
